import LoginCard from "../../components/auth/LoginCard";

const LoginPage = () => {
  return (
    <main className="flex min-h-screen items-center justify-center overflow-y-auto bg-background-blue px-[20px] py-[24px]">
      <LoginCard />
    </main>
  );
};

export default LoginPage;
